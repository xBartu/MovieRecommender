<h1>Roles</h1>
<% @roles.each do |role| %>
    <h3><%= role.title %></h3> - <%= link_to "Edit", edit_post_path(@post) %> - <%= link_to "Delete", post_path(@post), method: :delete, data: {confirm: "Are you sure?"}%>
<% end %>
