<% content_for(:title,  " Movies") %>
<div class="row">
  <% @movies.each do |movie| %>
    <div class="col-md-4">
      <img src="<%= movie.photo %>">
      <h2 class="movie-title"><%= movie.title %></h2>
      <p><%= movie.desc.first(400) %>...</p>
      <p><a class="btn btn-info" href="/movies/<%= movie.id %>" role="button">View Details</a></p>
    </div>
  <% end %>
</div>