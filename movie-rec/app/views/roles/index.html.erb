<h1>Roles</h1>
<%= link_to "Add a role", '/roles/new'%>
<% @roles.each do |role| %>
    <h3><%= role.title %></h3> - <%= link_to "Edit", edit_role_path(@post) %> - <%= link_to "Delete", role_path(@post), method: :delete, data: {confirm: "Are you sure?"}%>
<% end %>
