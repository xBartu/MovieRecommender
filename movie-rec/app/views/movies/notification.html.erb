<% content_for(:title,  "Notificiations" ) %>
<table class="table table-hover">
  <thead>
  </thead>
  <tbody>
  	<% @newmovies.each do |movie| %>
  	<tr>
      <th scope="row"><a href="/movies/<%= movie.title%>/"><%= movie.title%></a> is added on <%= movie.genre.name%></th>
    </tr>
	<% end %>
  </tbody>
<table>
<hr>