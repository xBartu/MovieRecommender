<!DOCTYPE html>
<html>
  <head>
    <title>MovieRec</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
      <nav class="navbar navbar-light bg-light">
        <a class="navbar-brand mb-0 h1" href="/">MovieRec</a>
        <ul>
          <% if user_signed_in? %>
              Hello <strong><%= current_user.email %></strong>.
              <a href="/movies/notification">Notifications</a> |
              <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> |
              <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %> 
            <% else %>
                <a class="navbar-link" href="/users/sign_in">Login</a>
                <a class="navbar-link" href="/users/sign_up">Register</a>
            <% end %>
        </ul>
      </nav>     
      <p class="navbar-text pull-right">
      <%= yield %>
      </p>
    </div>
  </body>
</html>
